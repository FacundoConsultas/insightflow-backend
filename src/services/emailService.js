// src/services/emailService.js
export const enviarAlertaEmail = async (usuario_id, categoria, detalle) => {
    console.log(`📧 Simulando envío de email para crisis de ${categoria}...`);
    return true;
};